{
  "name": "iles-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "iles dev --open",
    "build": "iles build",
    "preview": "vite preview --open --port 5050",
    "typecheck": "vue-tsc --noEmit"
  },
  "engines": {
    "node": "^14.0.0 || >= 16.0.0"
  },
  "devDependencies": {
    "iles": "^0.6.5",
    "typescript": "~4.5.2",
    "vite": "^2.6.14",
    "vue-tsc": "^0.29.6"
  }
}
